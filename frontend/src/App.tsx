import React from 'react';

import App from './App'; // Your main app component

const Root = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default Root;
